# MCP Extension Design

This folder contains design-only MCP metadata for Specification-Driven Coding.

It does not implement a runtime MCP server, remote registry, or auto-install behavior. Future MCP work must preserve approval gates, audit logs, decision matrices, and scorecards.

See `extensions/mcp/SPEC.md`.
