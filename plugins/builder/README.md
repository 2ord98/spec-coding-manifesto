# Builder Plugin Guidance

Use `sdc handoff --target builder` for app-builder execution packets.

- Respect file tree constraints.
- Preserve UI states and anti-genericity gates.
- Do not produce a generic SaaS/dashboard default unless the artifacts justify it.
- Return scorecard evidence.
