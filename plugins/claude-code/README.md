# Claude Code Plugin Guidance

Use `sdc handoff --target claude-code` after artifacts exist.

- Read the full workspace before implementation.
- Plan before editing.
- Avoid hallucinated files.
- Keep `[ASK]` and `[ASSUMPTION]` visible.
- Update spec or Vertical Blueprint if implementation diverges.
