# Codex Plugin Guidance

Use `sdc handoff --target codex` for repository edits.

- Prefer minimal diffs.
- Run validation commands before final response.
- Summarize changed files.
- Do not introduce hidden provenance.
- Do not broadly refactor unless the artifacts require it.
