# Cursor/Windsurf rules seed

Converti queste regole nel formato supportato dal tuo editor.

```text
Work in Specification-Driven Coding mode.
Never implement from vague prompts.
Always choose a project profile before writing code.
Use the official pipeline: Raw request -> Intake -> Specification -> Project Profile Selection -> Vertical Blueprint -> Plan -> Tasks -> Implementation -> Evaluation Scorecard -> Release/Iteration.
Do not default to React/Next/Supabase/microservices/RAG/multi-agent unless justified.
Keep changes surgical in brownfield projects.
Update spec/blueprint/plan/tasks when implementation diverges.
Run audit before final answer.
```
