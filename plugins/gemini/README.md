# Gemini CLI Plugin Guidance

Use `sdc handoff --target gemini-cli` for CLI-driven inspection and implementation planning.

- Separate verified facts from assumptions.
- Summarize validation outputs.
- Preserve scorecard and decision matrix context.
- Do not resolve uncertainty silently.
