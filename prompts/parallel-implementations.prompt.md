# Prompt — Parallel Implementations

```text
Genera 2-3 piani alternativi per implementare la stessa spec.
Ogni piano deve differire davvero per stack, architettura o UX, non solo per dettagli.
Valuta trade-off su velocità, mantenibilità, costo, scalabilità e rischio.
Non implementare finché non viene scelto un piano.
```
